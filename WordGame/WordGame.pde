void setup() {
  size(400,600) ;
  background(255) ;
  Word a = new Word("aba") ;
  if (a.word.length() > 0) {
    // just for testing purposes
    fill(255,255,0) ;
    ellipse(100,100,500,500) ;
  }
  // soup bowl design added here
  fill(255,125,0) ;
  ellipse(200,300,350,350) ;
}

void draw() {
  
}
